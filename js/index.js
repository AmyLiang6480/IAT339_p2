function addToCart(type) {
  const name = type === 0 ? 'New Levels New Devils' : 'We Out Here'
  alert(`'${name}' was added to your cart!`)
}

function submitReview() {
  alert(`Your review was submitted!`)
}